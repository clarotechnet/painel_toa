{
  "name": "dominium-toa-cloud-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/core.js",
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply dominium-toa-bridge --local",
    "db:remote": "wrangler d1 migrations apply dominium-toa-bridge --remote"
  },
  "devDependencies": {
    "wrangler": "^4.124.0"
  },
  "optionalDependencies": {
    "@cloudflare/workerd-windows-64": "1.20260815.1"
  }
}
