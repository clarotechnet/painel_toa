{
  "name": "dominium-toa-tec1",
  "version": "2.1.5",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "start": "cmd /d /c Abrir_Painel_TOA.cmd",
    "stop": "cmd /d /c Parar_Painel_TOA.cmd",
    "dev": "vite --host 0.0.0.0",
    "build": "node scripts/build.mjs",
    "preview": "vite --host 0.0.0.0",
    "test": "npm run test:web && npm run test:extension && npm run test:bridge && npm run test:cloud-bridge && npm run test:python",
    "test:web": "node tests/smoke.mjs && node tests/alert-service.mjs && node tests/xlsx-export.mjs",
    "test:extension": "node toa-discovery/test.js && node toa-discovery/service-worker.test.js",
    "test:bridge": "node toa-bridge/test-toa-inventory-core.js && node toa-bridge/test-disconnect-inventory.js && node toa-bridge/test-toa-route-tree.cjs && node toa-bridge/test-location-route-parser.js && node toa-bridge/test-cloud-bridge.js",
    "test:cloud-bridge": "node --test toa_cloud_bridge/test/*.test.mjs",
    "test:python": "python -B -m unittest discover -s tests -p test_*.py",
    "serve": "python app.py --no-browser"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  },
  "dependencies": {
    "firebase": "^12.17.1"
  }
}
